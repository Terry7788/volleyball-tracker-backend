# Profile selection - Railway will set this to 'prod'
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Default configuration (used when no profile specified)
spring.application.name=scoretracker